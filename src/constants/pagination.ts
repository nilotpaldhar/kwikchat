/**
 * Default page number for paginated queries.
 * This represents the starting page number when fetching paginated data.
 */
export const DEFAULT_PAGE = 1;

/**
 * Default page size for paginated queries.
 * This represents the number of items to fetch per page when querying paginated data.
 */
export const DEFAULT_PAGE_SIZE = 10;
